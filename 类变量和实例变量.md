### 类变量和实例变量

​	类变量会在所有实例中共享，实例中查找属性会先从实例变量中查看，如果没有在查找类变量中同名的。实例变量的修改只会在当前实例中变化，对类变量的修改则会影响该类所有实例访问类变量的变化。

​	

```
class A:
	num = 10
	def __init__(self):
		self.num = 20
		
A.unm #访问类变量
a = A()
a.num # 首先访问 self.num，如果没有，则访问A.num


```

