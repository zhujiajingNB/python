同步

​	一个任务的完成需要依赖于另一个任务，即当执行A操作后才能够执行B.

异步

​	任务的完成不依赖于其他任务的执行，即分配A和B同时执行任务。

阻塞与非阻塞

​	都是针对于等待消息通知。

同步阻塞

​	在等待A执行的过程时，不能做其他操作。

同步非阻塞

​	在等待A执行的过程中，可以执行其他操作，但同时要不停的查看A执行的情况，同样会造成程序不停的切		换，影响性能

异步阻塞

​	异步阻塞只是指等待任务处理信息的阻塞，而不是指处理任务A的阻塞



​	