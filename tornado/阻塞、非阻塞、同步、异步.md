1. 并发

   ​	单位时间内，运行的程序数量。即cpu同一时刻，只能运行一个程序。

2. 并行

   ​	同一时刻，运行的程序数量。即cpu同一时刻，能运行多个程序。

3. 同步

   ​	代码调用io操作时，必须等待io操作完成才返回的调用方式。

4. 异步

   ​	代码调用io操作时，不必等待io操作完成就返回的调用方式。

5. 阻塞

   ​	函数调用时，线程被挂起。

6. 非阻塞

   ​	函数调用时当前线程不会被挂起，而是立即返回。