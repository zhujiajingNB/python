crsf

​	用户通过账号密码登陆A网站，此时用户开启类一个新网站B，该网站嵌入A网站的链接，当用户点击该链接时，因为携带cookie值，则可以模拟用户发送请求到A.

​	解决思路：

​		通常攻击的带有表单的操作，所以当用户登录成功后，每次响应的表单渲染时，会返回服务器根据该用户生成sessionID一个tocken认证，隐藏在表单中，用户每次提交表单时则会发送该tocken。而B网站构造的链接请求没有携带该tocken

​			